# Application-specific mrbgem
# Provides class methods only (no instance creation)

class <%= class_name %>
  # Class methods are defined in C extension (src/app.c)
  #
  # Available methods:
  # - <%= class_name %>.version => Integer
end
